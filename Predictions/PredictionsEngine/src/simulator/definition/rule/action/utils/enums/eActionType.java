package simulator.definition.rule.action.utils.enums;

public enum eActionType {
    INCREASE,
    DECREASE,
    CALCULATION,
    MULTIPLY,
    DIVIDE,
    CONDITION,
    SET,
    KILL,
    REPLACE,
    PROXIMITY
}
