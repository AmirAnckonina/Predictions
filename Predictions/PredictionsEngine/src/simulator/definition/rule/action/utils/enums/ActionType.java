package simulator.definition.rule.action.utils.enums;

public enum ActionType {
    INCREASE,
    DECREASE,
    CALCULATION,
    MULTIPLY,
    DIVIDE,
    CONDITION,
    SET,
    KILL,
    REPLACE,
    PROXIMITY
}
